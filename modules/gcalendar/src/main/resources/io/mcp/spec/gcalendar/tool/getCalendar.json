{
  "name": "getCalendar",
  "description": "Get metadata for a calendar (Calendars.get)",
  "inputSchema": {
    "type": "object",
    "properties": {
      "calendarId": {
        "type": "string",
        "description": "Calendar ID (primary or other)"
      }
    },
    "required": ["calendarId"]
  }
}
