{
  "name": "listCalendars",
  "description": "List calendars available to the authenticated user (CalendarList.list)",
  "inputSchema": {
    "type": "object",
    "properties": {
      "maxResults": {
        "type": "integer",
        "minimum": 1,
        "maximum": 250,
        "description": "Maximum number of calendars to return (1-250)"
      },
      "pageToken": {
        "type": "string",
        "description": "Token for the next page of results"
      }
    }
  }
}
